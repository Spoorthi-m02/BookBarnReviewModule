import { Routes } from '@angular/router';
import { ReviewFormComponent } from './review-form/review-form.component';

export const routes: Routes = [{ path: 'review-form', component: ReviewFormComponent }];
